---
const { name, address, hours, access, image, mapUrl } = Astro.props;
---

<article class="bg-white shadow rounded-2xl overflow-hidden hover:shadow-lg transition">
  <img src={image} alt={name} class="w-full h-48 object-cover" />
  <div class="p-4">
    <h2 class="text-xl font-semibold text-indigo-700 mb-2">{name}</h2>
    <p class="text-sm text-gray-600 mb-1">📍 {address}</p>
    <p class="text-sm text-gray-600 mb-1">🕒 {hours}</p>
    <p class="text-sm text-gray-600 mb-3">🚃 {access}</p>
    <a href={mapUrl} target="_blank" rel="noopener noreferrer" class="text-indigo-500 font-medium hover:underline">
      Googleマップで見る
    </a>
  </div>
</article>
