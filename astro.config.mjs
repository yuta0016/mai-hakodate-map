// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://yuta0016.github.io', // 必ずあなたのユーザー名に置き換えてください
    base: '/mai-hakodate-map', // 必ずあなたのリポジトリ名に置き換えてください
});
