// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://yuta0016.github.io',
    base: '/mai-hakodate-map',
    output: 'static',
});
